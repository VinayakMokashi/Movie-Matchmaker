# Movie Matchmaker — dependencies
# Standard data-science stack; no ML framework needed (pure pandas correlation).

pandas>=1.3
numpy>=1.21
matplotlib>=3.4
seaborn>=0.11

# Notebook was developed with Python 3.9. To run it:
#   pip install -r requirements.txt
#   jupyter notebook Movie_Recommender.ipynb
jupyter>=1.0
